<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="FEN-Bloat"
        MinWidth="900" MinHeight="600"
        Width="1050" Height="680"
        ResizeMode="CanResize"
        SnapsToDevicePixels="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource BgColor}"
        Foreground="{DynamicResource FgColor}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome ResizeBorderThickness="5" CaptionHeight="32" CornerRadius="8" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border x:Name="MainBorder" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Background="{DynamicResource BgColor}" Margin="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" x:Name="TitleBar">
                <Border x:Name="TitleBarBackground" Background="{DynamicResource BgColor}" CornerRadius="8,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Logo area -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0">
                            <Image x:Name="LogoImage" Width="22" Height="22" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="FEN-Bloat" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock x:Name="VersionText" Text=" v1.0.0" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" FontSize="11" Opacity="0.6"/>
                        </StackPanel>

                        <!-- Title bar buttons -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button x:Name="ThemeToggleBtn" shell:WindowChrome.IsHitTestVisibleInChrome="True" Content="&#xE790;" FontFamily="Segoe Fluent Icons" FontSize="13" Style="{StaticResource TitlebarButton}" ToolTip="Toggle theme"/>
                            <Button x:Name="CloseBtn" shell:WindowChrome.IsHitTestVisibleInChrome="True" Content="&#xE8BB;" Style="{StaticResource CloseButton}" ToolTip="Close"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1" x:Name="ContentGrid">
                <!-- Tab Control with custom styled headers -->
                <TabControl x:Name="MainTabControl" Background="Transparent" BorderThickness="0" Padding="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border x:Name="Border" Background="Transparent" BorderThickness="0" Padding="16,10" Margin="0,0,0,0" CornerRadius="0">
                                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.FontSize="13" TextElement.FontWeight="SemiBold"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonBg}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="False">
                                                <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource SecondaryButtonHover}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>

                    <!-- ==================== TAB: Debloat ==================== -->
                    <TabItem Header="Debloat" x:Name="TabDebloat">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Tab Header -->
                            <StackPanel Grid.Row="0" Margin="24,16,24,8">
                                <TextBlock Text="Debloat" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource FgColor}"/>
                                <TextBlock Text="Select apps to remove and privacy features to disable" FontSize="13" Foreground="{DynamicResource FgColor}" Opacity="0.7" Margin="0,2,0,8"/>
                            </StackPanel>

                            <!-- Scrollable content with 3 columns -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="24,0,24,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Col 0, Row 0: App Removal -->
                                    <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE74C;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="App Removal" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="AppRemovalPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Col 1, Row 0: Privacy -->
                                    <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE72E;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Privacy &amp; Telemetry" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="PrivacyPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Col 2, Row 0: AI Features -->
                                    <Border Grid.Column="2" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE90A;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="AI Features" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="AIPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Col 0, Row 1: Interface -->
                                    <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE774;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Interface" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="InterfacePanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Col 1, Row 1: Gaming -->
                                    <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE7FC;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Gaming" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="GamingPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Col 2, Row 1: System -->
                                    <Border Grid.Column="2" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE770;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="System" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="SystemPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>

                            <!-- Bottom action bar -->
                            <Border Grid.Row="2" Background="{DynamicResource CardBgColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="24,12">
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="DebloatApplyBtn" Content="Apply Selected" Style="{StaticResource PrimaryButtonStyle}" Width="160" Height="40" FontSize="15"/>
                                    <Button x:Name="DebloatSelectAllBtn" Content="Select All" Style="{StaticResource SecondaryButtonStyle}" Width="110" Height="40" Margin="12,0,0,0"/>
                                    <Button x:Name="DebloatSelectRecommendedBtn" Content="Recommended" Style="{StaticResource SecondaryButtonStyle}" Width="130" Height="40" Margin="8,0,0,0"/>
                                    <TextBlock x:Name="DebloatStatusText" Text="" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{DynamicResource FgColor}" Opacity="0.7"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>

                    <!-- ==================== TAB: Install ==================== -->
                    <TabItem Header="Install" x:Name="TabInstall">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="24,16,24,8">
                                <TextBlock Text="Install Applications" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource FgColor}"/>
                                <TextBlock Text="Install useful programs via WinGet" FontSize="13" Foreground="{DynamicResource FgColor}" Opacity="0.7" Margin="0,2,0,8"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="24,0,24,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE774;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Essentials" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                            </StackPanel>
                                            <StackPanel x:Name="InstallEssentialsPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE90F;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Utilities" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                            </StackPanel>
                                            <StackPanel x:Name="InstallUtilitiesPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="2" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE7FC;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Gaming" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                            </StackPanel>
                                            <StackPanel x:Name="InstallGamingPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE97F;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Dev Tools" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                            </StackPanel>
                                            <StackPanel x:Name="InstallDevPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE8B9;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Creative" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                            </StackPanel>
                                            <StackPanel x:Name="InstallCreativePanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>

                            <Border Grid.Row="2" Background="{DynamicResource CardBgColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="24,12">
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="InstallApplyBtn" Content="Install Selected" Style="{StaticResource PrimaryButtonStyle}" Width="160" Height="40" FontSize="15"/>
                                    <Button x:Name="InstallUpdateAllBtn" Content="Update All" Style="{StaticResource SecondaryButtonStyle}" Width="120" Height="40" Margin="12,0,0,0"/>
                                    <TextBlock x:Name="InstallStatusText" Text="" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{DynamicResource FgColor}" Opacity="0.7"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>

                    <!-- ==================== TAB: Tweaks ==================== -->
                    <TabItem Header="Tweaks" x:Name="TabTweaks">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="24,16,24,8">
                                <TextBlock Text="Tweaks" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource FgColor}"/>
                                <TextBlock Text="Advanced system tweaks and optimizations" FontSize="13" Foreground="{DynamicResource FgColor}" Opacity="0.7" Margin="0,2,0,8"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="24,0,24,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE72E;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Privacy" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="TweaksPrivacyPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE770;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="System" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="TweaksSystemPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="2" Grid.Row="0" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE774;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Explorer" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="TweaksExplorerPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE90A;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="AI" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="TweaksAIPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource CategoryCardBorderStyle}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE774;" FontFamily="Segoe Fluent Icons" FontSize="19" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <TextBlock Text="Taskbar" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button ToolTip="More info" Style="{StaticResource CategoryHelpLinkButtonStyle}">
                                                    <TextBlock Text="(?)" Style="{StaticResource CategoryHelpLinkTextStyle}"/>
                                                </Button>
                                            </StackPanel>
                                            <StackPanel x:Name="TweaksTaskbarPanel" Orientation="Vertical"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>

                            <Border Grid.Row="2" Background="{DynamicResource CardBgColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="24,12">
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="TweaksApplyBtn" Content="Apply Tweaks" Style="{StaticResource PrimaryButtonStyle}" Width="160" Height="40" FontSize="15"/>
                                    <TextBlock x:Name="TweaksStatusText" Text="" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{DynamicResource FgColor}" Opacity="0.7"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>

                    <!-- ==================== TAB: Tools ==================== -->
                    <TabItem Header="Tools" x:Name="TabTools">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="24,16,24,8">
                                <TextBlock Text="Tools" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource FgColor}"/>
                                <TextBlock Text="System maintenance and repair utilities" FontSize="13" Foreground="{DynamicResource FgColor}" Opacity="0.7" Margin="0,2,0,8"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="24,0,24,0">
                                <StackPanel>
                                    <!-- Row of tool buttons -->
                                    <WrapPanel>
                                        <Border Style="{StaticResource CategoryCardBorderStyle}" Width="300">
                                            <Button x:Name="ToolRepairWU" Content="&#xE770;  Repair Windows Update" Style="{StaticResource SecondaryButtonStyle}" Width="268" Height="50" FontSize="14" HorizontalAlignment="Left"/>
                                        </Border>
                                        <Border Style="{StaticResource CategoryCardBorderStyle}" Width="300">
                                            <Button x:Name="ToolSFC" Content="&#xE946;  Run SFC &amp; DISM" Style="{StaticResource SecondaryButtonStyle}" Width="268" Height="50" FontSize="14" HorizontalAlignment="Left"/>
                                        </Border>
                                        <Border Style="{StaticResource CategoryCardBorderStyle}" Width="300">
                                            <Button x:Name="ToolCleanTemp" Content="&#xE74D;  Clean Temporary Files" Style="{StaticResource SecondaryButtonStyle}" Width="268" Height="50" FontSize="14" HorizontalAlignment="Left"/>
                                        </Border>
                                        <Border Style="{StaticResource CategoryCardBorderStyle}" Width="300">
                                            <Button x:Name="ToolRestorePoint" Content="&#xE74C;  Create Restore Point" Style="{StaticResource SecondaryButtonStyle}" Width="268" Height="50" FontSize="14" HorizontalAlignment="Left"/>
                                        </Border>
                                        <Border Style="{StaticResource CategoryCardBorderStyle}" Width="300">
                                            <Button x:Name="ToolMicroWin" Content="&#xE74E;  MicroWin (ISO Creator)" Style="{StaticResource SecondaryButtonStyle}" Width="268" Height="50" FontSize="14" HorizontalAlignment="Left"/>
                                        </Border>
                                        <Border Style="{StaticResource CategoryCardBorderStyle}" Width="300">
                                            <Button x:Name="ToolOpenLogs" Content="&#xF000;  Open Logs Folder" Style="{StaticResource SecondaryButtonStyle}" Width="268" Height="50" FontSize="14" HorizontalAlignment="Left"/>
                                        </Border>
                                    </WrapPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>
                </TabControl>

                <!-- Progress Bar at bottom of content -->
                <ProgressBar x:Name="MainProgressBar" Grid.Row="1" Height="3" VerticalAlignment="Bottom" Style="{StaticResource ApplyProgressBarStyle}" Visibility="Hidden"/>
            </Grid>
        </Grid>
    </Border>
</Window>
